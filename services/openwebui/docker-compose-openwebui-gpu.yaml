# This file has been replaced by separate compose files under:
#   services/openwebui-gpu/docker-compose-openwebui-gpu.yaml
#   services/comfyui/docker-compose-comfyui.yaml
#   services/stable-diffusion/docker-compose-stable-diffusion.yaml
#
# To use these services, include the relevant file(s) in your COMPOSE_FILE variable in .env
# or remove them by excluding the corresponding entries.
